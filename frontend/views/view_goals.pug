extends layout

block content

  h1.viewGoals #{user}'s - #{title}

  if goals && goals.length
    .goalsContainer

      each goal in goals
        .goalBox
          h3 #{goal.title}
          p #{goal.description}
          p #{goal.deadline}
          
          .goal-actions
            a(href=`/api/goals/shared/${goal._id}`, class="btn share-btn") Share Goal
            a(href=`/api/goals/delete/${goal._id}`, class="btn delete-btn") Delete Goal

  else
    p.noGoals No goals found.
